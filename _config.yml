# ⚠️ Этот файл НЕ используется при сборке сайта.
#
# Сайт собирается GitHub Actions (.github/workflows/deploy.yml) из двух
# независимых Jekyll-сайтов:
#   - ru/_config.yml — русская версия (основная)
#   - en/_config.yml — английская версия (перевод)
# Плюс статичный лендинг index.html в корне.
#
# Настройки меняйте в ru/_config.yml и en/_config.yml.
